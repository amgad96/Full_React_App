deployApp.groovy File:
  The deployApp.groovy file located in the vars directory is used for the Jenkins shared library.
Note:
  You will need to add the repo url and branch name in the jenkins shared library configuration. As in the issue named "Shared library configuration on Jenkins server".
